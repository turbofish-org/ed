[package]
name = "ed"
version = "0.3.0"
authors = ["Turbofish <team@turbofish.org>"]
edition = "2018"
description = "A minimalist crate for deterministic binary encodings in Rust."
license = "Apache-2.0"

[dependencies]
ed-derive = { version = "0.3.0", path = "derive" }
thiserror = "1.0"

[profile.bench]
lto = true

[profile.release]
lto = true
